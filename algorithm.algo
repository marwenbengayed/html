ALGORITHM sum
VAR
    a: INTEGER ;
    b: INTEGER ;
    c: INTEGER ;
    sum: INTEGER :=0 ;
BEGIN
Read (a)
Read (b)
Read (c)
sum:= a + b+ c 
write (sum)
END
ALGORITHM count
VAR
n: INTEGER ;
    count : INTEGER := 0 ;
BEGIN
write (n);
  while ( n mod  2 ) do
  n:= n/2 ;
  count := count+1;
 
END